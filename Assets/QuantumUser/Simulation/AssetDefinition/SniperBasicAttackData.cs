namespace Quantum
{
    [System.Serializable]
    public unsafe partial class SniperBasicAttackData : HitscanAttackData
    {
        // The Sniper basic attack currently doesn't need any extra logic. Only it's parent class logic is necessary
        // We still created this asset as to make it class specific, but that's not mandatory
    }
}